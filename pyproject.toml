[project]
name = "alexa_manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
classifiers = ["Private :: Do Not Upload"]
dependencies = [
    "pre-commit>=4.2.0",
    "requests>=2.31.0",
]

